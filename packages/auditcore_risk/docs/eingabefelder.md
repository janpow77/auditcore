# Eingabefelder der Regelprofile

<!-- Erzeugt von tools/document_fields.py – nicht von Hand bearbeiten. -->

Erzeugt mit `python tools/document_fields.py` aus allen Profilen in
`src/auditcore_risk/profile_data`; `tests/test_eingabefelder.py` schlägt fehl,
sobald Profile und diese Datei auseinanderlaufen. Welche Felder eine Regel liest,
folgt aus den Parametern ihrer Regelart; das Verhalten bei fehlenden Daten aus
`requires`/`when_missing_columns`, `missing_value`, `missing_amount_reason`,
`column_missing_value`, `id_column_missing`, `relevance.column_missing` und der
Mechanik der Regelart (`rules.py`, `invoice_rules.py`, `score_rules.py`).

* **Spalte fehlt**: kein Datensatz hat den Schlüssel bzw. die Spalte steht nicht
  in `columns` (beim DataFrame: nicht in `frame.columns`).
* **leer**: `None`, `NaN`, `pandas.NA` oder `NaT` in einem Datensatz, bei
  Datensätzen ohne den Schlüssel ebenso.
* **Pflicht**: ohne die Spalte bricht die Auswertung mit `InputError` ab (bei
  „auch der Wert“ ebenso bei einem leeren Wert). **optional**: die Auswertung
  läuft, das Verhalten steht in der letzten Spalte (übersprungen, kein Merkmal,
  unbestimmt oder Ersatzwert).
* „Bedeutung laut Quelle nicht dokumentiert“: keine belegbare Beschreibung in der Quelle.

Bedeutungen stammen aus dem Quellcode der Anwendungen zum charakterisierten
Stand (Commit wie angegeben), dem Profil selbst oder der Paketdokumentation.

## Profile

| Profil | Version | Status | Felder | Regeln |
|---|---|---|---|---|
| [`audit_designer.flowstat_belegliste`](#audit_designerflowstat_belegliste-1254591156d3) | `1254591156d3` | `LEGACY_CHARACTERIZED` | 10 | 10 |
| [`flowinvoice.exante_basis`](#flowinvoiceexante_basis-fb2d18568d2e) | `fb2d18568d2e` | `LEGACY_CHARACTERIZED` | 6 | 7 |
| [`flowinvoice.exante_heuristik`](#flowinvoiceexante_heuristik-fb2d18568d2e) | `fb2d18568d2e` | `LEGACY_CHARACTERIZED` | 8 | 14 |
| [`flowinvoice.rbvk_wibank`](#flowinvoicerbvk_wibank-2026092) | `2026.09.2` | `APPROVED` | 20 | 26 |
| [`flowinvoice.rbvk_wibank`](#flowinvoicerbvk_wibank-fb2d18568d2e) | `fb2d18568d2e` | `LEGACY_CHARACTERIZED` | 20 | 25 |
| [`flowinvoice.risk_checker`](#flowinvoicerisk_checker-2026092) | `2026.09.2` | `APPROVED` | 17 | 9 |
| [`flowinvoice.risk_checker`](#flowinvoicerisk_checker-2026093) | `2026.09.3` | `APPROVED` | 17 | 9 |
| [`flowinvoice.risk_checker`](#flowinvoicerisk_checker-fb2d18568d2e) | `fb2d18568d2e` | `LEGACY_CHARACTERIZED` | 17 | 9 |
| [`riskanalysis.legacy`](#riskanalysislegacy-b5c523bf7eaa) | `b5c523bf7eaa` | `LEGACY_CHARACTERIZED` | 15 | 10 |
| [`riskanalysis.year_bound`](#riskanalysisyear_bound-2026091) | `2026.09.1` | `CANDIDATE_HUMAN_DECISION_REQUIRED` | 16 | 10 |
| [`riskanalysis.year_bound`](#riskanalysisyear_bound-2026092) | `2026.09.2` | `APPROVED` | 17 | 10 |
| [`riskanalysis.year_bound`](#riskanalysisyear_bound-2026093) | `2026.09.3` | `APPROVED` | 17 | 10 |
| [`riskanalysis.year_bound`](#riskanalysisyear_bound-2026094) | `2026.09.4` | `APPROVED` | 17 | 10 |
| [`riskanalysis.year_bound`](#riskanalysisyear_bound-2026095) | `2026.09.5` | `APPROVED` | 17 | 10 |

## audit_designer.flowstat_belegliste 1254591156d3

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `7dfc1311e7db21b3af199b6727bc9ec182062285423ee6e2b9ec73426dde313e`.
Eingabevertrag laut Profil: Spalten wie nach normalize_belegliste (Beträge als Zahl, Datumsfelder als Datum).

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `anerkannter_betrag` | BL_RF07_ACCEPTED_MISMATCH – Anerkannter Betrag ≠ Projektbetrag − Kürzung (Subtrahend) | Anerkannter Betrag; Quellspalten „anerkannter Betrag“, „anerkannt“, „accepted amount“ (Rolle `AMOUNT_ACCEPTED`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF07_ACCEPTED_MISMATCH: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0`; nicht numerischer Text gilt als leer |
| `direktvergabe` | BL_RF09_DIRECT_AWARD_HIGH_AMOUNT – Direktvergabe über 25.000 (Kennzeichen (Muster „ja\|true\|1“)) | Kennzeichen Direktvergabe; Quellspalten „Direktvergabe“, „Freihändige Vergabe“, „direct award“ (Rolle `DIRECT_AWARD_FLAG`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF09_DIRECT_AWARD_HIGH_AMOUNT: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → kein Treffer (kein Merkmal) |
| `kuerzungsbetrag` | BL_RF06_CUT_WITHOUT_REASON – Kürzung ohne Kürzungsgrund (Betrag)<br>BL_RF07_ACCEPTED_MISMATCH – Anerkannter Betrag ≠ Projektbetrag − Kürzung (Subtrahend) | Kürzungsbetrag; Quellspalten „Kuerzungsbetrag“, „Kürzungsbetrag“, „Abweichung“, „Korrektur“ (Rolle `AMOUNT_DEVIATION`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF06_CUT_WITHOUT_REASON: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0`; nicht numerischer Text gilt als leer<br>BL_RF07_ACCEPTED_MISMATCH: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0`; nicht numerischer Text gilt als leer |
| `kuerzungsgrund` | BL_RF06_CUT_WITHOUT_REASON – Kürzung ohne Kürzungsgrund (Begründungstext) | Kürzungsgrund; Quellspalten „Kuerzungsgrund“, „Kürzungsgrund“, „Korrekturgrund“, „Feststellungsgrund“ (Rolle `CUT_REASON`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF06_CUT_WITHOUT_REASON: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → gilt als fehlend (Merkmal bei Betrag ≠ 0) |
| `projektbetrag` | BL_RF01_ROUND_AMOUNT – Runder Tausenderbetrag (Betrag)<br>BL_RF02_NEAR_THRESHOLD – Betrag knapp unter Vergabeschwelle (Betrag)<br>BL_RF05_DUPLICATE_INVOICE – Doppelte Rechnung (Schlüsselteil)<br>BL_RF07_ACCEPTED_MISMATCH – Anerkannter Betrag ≠ Projektbetrag − Kürzung (Minuend)<br>BL_RF08_PROCUREMENT_MISSING – Betrag > 25.000 ohne Vergabeangabe (Betrag)<br>BL_RF09_DIRECT_AWARD_HIGH_AMOUNT – Direktvergabe über 25.000 (Betrag)<br>BL_RF10_VENDOR_CONCENTRATION – Konzentration auf einen Rechnungssteller (≥ 50 % der Summe) (Betrag (Anteil)) | Projektbezogener Betrag des Belegs; Quellspalten „projektbez. Betrag“, „projektbezogener Betrag“, „Projektbetrag“ (Rolle `AMOUNT_PROJECT`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF01_ROUND_AMOUNT: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0` (kein Merkmal); nicht numerischer Text gilt als leer<br>BL_RF02_NEAR_THRESHOLD: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0` (kein Merkmal); nicht numerischer Text gilt als leer<br>BL_RF05_DUPLICATE_INVOICE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → leere Werte gelten untereinander als gleich<br>BL_RF07_ACCEPTED_MISMATCH: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0`; nicht numerischer Text gilt als leer<br>BL_RF08_PROCUREMENT_MISSING: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0` (kein Merkmal); nicht numerischer Text gilt als leer<br>BL_RF09_DIRECT_AWARD_HIGH_AMOUNT: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Ersatzwert `0.0` (kein Merkmal); nicht numerischer Text gilt als leer<br>BL_RF10_VENDOR_CONCENTRATION: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → nicht gezählt (auch nicht numerischer Text) |
| `rechnungsdatum` | BL_RF04_PAYMENT_BEFORE_INVOICE – Zahlung vor Rechnungsdatum (Vergleichsdatum) | Rechnungsdatum; Quellspalten „Rechnungsdatum“, „Belegdatum“, „Invoice Date“ (Rolle `DATE_INVOICE`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF04_PAYMENT_BEFORE_INVOICE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → kein Merkmal; Text, der kein ISO-Datum ist → Abbruch (`InputError`) |
| `rechnungsnummer` | BL_RF05_DUPLICATE_INVOICE – Doppelte Rechnung (Schlüsselteil) | Rechnungsnummer bzw. externe Referenz; Quellspalten „Rechnungsnummer / ext. Referenz“, „Rechnungsnummer“, „ext. Referenz“, „Externe Referenz“, „Rechnung Nr“ (Rolle `INVOICE_REFERENCE`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF05_DUPLICATE_INVOICE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → leere Werte gelten untereinander als gleich |
| `rechnungssteller` | BL_RF05_DUPLICATE_INVOICE – Doppelte Rechnung (Schlüsselteil)<br>BL_RF10_VENDOR_CONCENTRATION – Konzentration auf einen Rechnungssteller (≥ 50 % der Summe) (Gruppe) | Rechnungssteller; Quellspalten „Rechnungssteller“, „Auftragnehmer“, „Kreditor“, „Lieferant“, „Vendor“ (Rolle `VENDOR_NAME`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF05_DUPLICATE_INVOICE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → leere Werte gelten untereinander als gleich<br>BL_RF10_VENDOR_CONCENTRATION: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → eigene Gruppe „fehlend“ |
| `vergabe` | BL_RF08_PROCUREMENT_MISSING – Betrag > 25.000 ohne Vergabeangabe (Vergabekennung) | Vergabeangabe; Quellspalten „Vergabe“, „Vergabenummer“, „Vergabe-ID“ (Rolle `PROCUREMENT_REFERENCE`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF08_PROCUREMENT_MISSING: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, `"None"`) |
| `zahlungsdatum` | BL_RF03_MISSING_PAYMENT_DATE – Zahlungsdatum fehlt (Pflichtangabe)<br>BL_RF04_PAYMENT_BEFORE_INVOICE – Zahlung vor Rechnungsdatum (Datum (Merkmal, wenn vor dem Vergleichsdatum)) | Datum der Bezahlung; Quellspalten „Datum der Bezahlung“, „Zahlungsdatum“, „Wertstellung“, „Valuta“ (Rolle `DATE_PAYMENT`). Beleg: audit_designer@1254591 `backend/app/modules/flowstat/services/belegliste_analysis_service.py` (`ROLE_ALIASES`, `ROLE_CANONICAL_COLUMNS`, `normalize_belegliste`) | optional | BL_RF03_MISSING_PAYMENT_DATE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → Merkmal<br>BL_RF04_PAYMENT_BEFORE_INVOICE: Spalte fehlt → Regel wird übersprungen (`Evaluation.skipped`); leer → kein Merkmal; Text, der kein ISO-Datum ist → Abbruch (`InputError`) |

## flowinvoice.exante_basis fb2d18568d2e

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `4e90c8f778602b488ba4989984a702941d1c0a72b25851ef70fbab2ddf8eea53`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `an_risiko` | E7 – Auftragnehmer-Risiko (> 30 %) (Zahl) | Anteil des Vorhaben-Bruttos an historisch auffällige Auftragnehmer (leave-one-out). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`an_risiko_loo`) | optional | E7: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `brutto` | E1 – Budget > 1 Mio € (Zahl)<br>E2 – Budget 500 T€–1 Mio € (Zahl) | Bruttobetrag je Vorhaben (Spalte `brutto` der Vorhabentabelle); Grundlage der Budget-Kriterien. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`_features`); flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | E1: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>E2: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `erw_ma` | E4 – Erwartete Mittelabrufe hoch (> 4) (Zahl) | Erwartete Anzahl Mittelabrufe ex ante = OLS(realisierte MA ~ Laufzeit + log Budget). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`erwartete_ma`) | optional | E4: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `fpgq` | E5 – FPG-Fehlerquote historisch hoch (> 15 %) (Zahl) | Historische Kürzungsquote der Förderprogrammgruppe (`kuerzungsquote` der FPG-Tabelle), fehlend 0. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`_features`) | optional | E5: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `grp_v` | E6 – Erstantragsteller (Zahl) | Anzahl Vorhaben der Gruppe des Begünstigten (`vorhaben` der Gruppentabelle), fehlend 1. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`_features`) | optional | E6: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `1`, Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `laufzeit_monate` | E3 – Geplante Laufzeit > 24 M (Zahl) | Geplante Laufzeit des Vorhabens in Monaten. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`erwartete_ma`) | optional | E3: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |

## flowinvoice.exante_heuristik fb2d18568d2e

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `5985e36487b147b09ef372ad963ce2f150afe1aff9be49ebfb359ee474cfc64a`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `belege` | H8 – Belege > 200 (Zahl)<br>H9 – Belege 100–200 (Zahl) | Anzahl Belege des Vorhabens (Spalte `belege` der Vorhabentabelle). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H8: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H9: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `brutto` | H1 – Budget > 1 Mio (Zahl)<br>H2 – Budget 500 T – 1 Mio (Zahl) | Bruttobetrag je Vorhaben (Spalte `brutto` der Vorhabentabelle); Grundlage der Budget-Kriterien. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`_features`); flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H1: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H2: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `fpg_quote` | H12 – FPG-Quote > 15 (Zahl)<br>H13 – FPG-Quote 2–15 (Zahl) | Kürzungsquote der Förderprogrammgruppe (`fpg_quote.get(fpg, 0)`). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H12: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H13: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `fpg_rueckgabequote` | H14 – FPG-Rückgabequote > 40 (Zahl) | Rückgabequote der Förderprogrammgruppe (`fpg_ret.get(fpg, 0)`). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H14: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `gruppe_vorhaben` | H10 – Gruppe mit höchstens einem Vorhaben (Zahl)<br>H11 – Gruppe mit mehr als drei Vorhaben (Zahl) | Anzahl Vorhaben der Gruppe des Begünstigten (`grp_vorhaben.get(gruppe, 1)`). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H10: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `1`, Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H11: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `1`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `kuerzungsquote` | H3 – Kürzungsquote > 20 (Zahl)<br>H4 – Kürzungsquote 10–20 (Zahl) | Kürzungsquote des Vorhabens (Spalte `kuerzungsquote` der Vorhabentabelle). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`heuristik_score`); `tools/capture_flowinvoice_verwk_scores.py` | optional | H3: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H4: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `laufzeit_monate` | H5 – Laufzeit > 36 Monate (Zahl)<br>H6 – Laufzeit 24–36 Monate (Zahl) | Geplante Laufzeit des Vorhabens in Monaten. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`erwartete_ma`) | optional | H5: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>H6: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `ma` | H7 – Mittelabrufe > 5 (Zahl) | Realisierte Anzahl Mittelabrufe des Vorhabens. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/exante_score.py` (`erwartete_ma`) | optional | H7: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |

## flowinvoice.rbvk_wibank 2026.09.2

Status `APPROVED`, Fingerabdruck `9f800f4e59b1c50641c4ca8074d1d24f3b10ce1916cc382c934390339535492b`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abruf_anteil` | K28 – Abrufanteil > 0,5 (Zahl) | Abrufanteil: Bruttobetrag des Mittelabrufs / `bewilligungsrahmen`; ohne Bewilligungsrahmen der übergebene Wert. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K28: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `beihilfefrei` | K7 – Beihilfefrei und Trennungsrechnung (Wahrheitswert) | Beihilfefrei; Aliase `beihilfefrei`, `STAATLBEIH`, `beihilfefreiheit`, ergänzt aus `beihilferegelung` („keine beihilfe\|beihilfefrei“). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K7: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `erstes_vorhaben` | K11 – Kein früheres Vorhaben des Begünstigten (Wahrheitswert) | Der Begünstigte hatte kein früheres Vorhaben (`len(vh) == 0` im Bewertungsschritt). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`); `tools/capture_flowinvoice_verwk_scores.py` | optional | K11: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `eu_vergaberelevant` | K24 – EU-weite Vergabe (Wahrheitswert) | EU-vergaberelevant; Aliase `eu_vergaberelevant`, `eu_vergabeverfahren`, `EU_VERGABE`, oder `vergabeverfahren` enthält „eu\|europa\|oberschwelle“, oder FPG aus `eu_vergaberelevante_fpg` bei Hochschule/Forschung. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K24: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `externe_kuerzung` | K12 – Kürzung aufgrund von Feststellungen der KOM, Rechnungshöfe oder Prüfbehörde (Zahl) | Kürzung aufgrund von Prüfungsfeststellungen der KOM, der Rechnungshöfe oder der Prüfbehörde; Aufbereitung beim Consumer. Beleg: Profil `open_decisions`; `docs/behavior-changes.md` (K11) | optional | K12: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `fpg` | K2 – FPG 1008/1009/1010 (Text) | Förderprogrammgruppe (Maßnahme, z. B. 956/960/973). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`) | optional | K2: Spalte fehlt → wie leerer Wert; leer → Schlüssel fehlt im Datensatz: Ersatztext `""` (kein Treffer); Wert `None`: Text „None“ (kein Treffer) |
| `hat_absch` | K4 – Abschreibung (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „abschreib\|afa\|wertminderung“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K4: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_bau` | K3 – Bau (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „bau\|gebäude\|anlage\|infrastruktur“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K3: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_sach` | K26 – Sachkosten (Wahrheitswert) | Mittelabruf enthält einen direkten Beleg (siehe `n_direct`), dessen Kostenart nicht „personal\|gemeinkost\|pauschal\|einnahm“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K26: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_sachleist` | K5 – Sachleistung (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „sachleist“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K5: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_vergabe` | K23 – Vergabe vorhanden (Wahrheitswert) | Mittelabruf enthält einen Beleg mit nicht leerer `vergabenummer`, oder er ist EU-vergaberelevant. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K23: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `n_direct` | K25 – Direkte Belege > 80 (Zahl) | Anzahl direkter Belege des Mittelabrufs: Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) ohne „gemeinkost\|pauschal\|sek\|standardeinheits\|personal“. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K25: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `oeffentlich_rechtlich` | K13 – Öffentlich-rechtlich (Wahrheitswert) | Öffentlich-rechtlicher Träger; Aliase `oeffentlich_rechtlich`, `RECHTSTRAEGER`, `rechtsform_oeffentlich`, oder Hochschule. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K13: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `oeffentlicher_auftraggeber` | K14 – Öffentlicher Auftraggeber (Wahrheitswert) | Öffentlicher Auftraggeber; Aliase `oeffentlicher_auftraggeber`, `oeff_auftraggeber`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K14: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `offene_auflagen_anzahl` | K10 – Bewilligte offene Auflagen (1. Auflage) (Zahl)<br>K10b – Bewilligte offene Auflagen (2. Auflage, score_max 2) (Zahl) | Anzahl offener Auflagen (Zahl); Aufbereitung beim Consumer. Beleg: Profil `open_decisions`; `docs/behavior-changes.md` (K11) | optional | K10: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K10b: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `projekt_budget` | K8 – Projektbudget 1,5 Mio – 5 Mio (Zahl)<br>K9 – Projektbudget > 5 Mio (Zahl) | Projektbudget: Maximum von `bewilligungsrahmen` (sonst `bruttobetrag`) je Vorhaben, ersatzweise die Bruttosumme des Mittelabrufs. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K8: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K9: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `trennungsrechnung_erforderlich` | K7 – Beihilfefrei und Trennungsrechnung (Wahrheitswert) | Trennungsrechnung erforderlich; Aliase `trennungsrechnung_erforderlich`, `trennungsrechnung`, `TRENNRECH`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K7: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `verbundvorhaben` | K1 – Verbundvorhaben (Wahrheitswert) | Verbundvorhaben; Aliase `verbundvorhaben`, `verbund`, `AKTZEIEXT2`, auch auf Belegebene. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K1: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `vorherige_kuerzungsgruende` | K20 – Vorherige Kürzungsgründe 1.1 bis 1.24 (Codeliste)<br>K21 – Vorherige Kürzungsgründe 5.1 oder 5.2 (Codeliste)<br>K22 – Vorherige Kürzungsgründe 8.1–8.3, 8.8 oder 13.1 (Codeliste) | Kürzungsgrund-Codes früherer Kürzungen (z. B. „1.3“, „13.1“), auch aus eigenen früheren Mittelabrufen; Aufbereitung beim Consumer. Beleg: Profil `open_decisions`; `docs/behavior-changes.md` (K11) | optional | K20: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`)<br>K21: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`)<br>K22: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`) |
| `vorherige_verwk_quote` | K16 – Schlechte Ergebnisse vorheriger Verwaltungskontrollen (Zahl)<br>K17 – Historische Differenz über 25 Prozent (Zahl)<br>K18 – Historische Differenz über 50 Prozent (Zahl)<br>K19 – Historische Differenz unter 5 Prozent (Zahl) | Quote aus eigenen früheren Verwaltungskontrollen (VerwK); Aufbereitung beim Consumer. Beleg: Profil `open_decisions`; `docs/behavior-changes.md` (K11) | optional | K16: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K17: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K18: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K19: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |

## flowinvoice.rbvk_wibank fb2d18568d2e

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `72b8cce180b2d94eac9d528815e900028b8c67621e567a4373286576f66ec482`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abruf_anteil` | K28 – Abrufanteil > 0,5 (Zahl) | Abrufanteil: Bruttobetrag des Mittelabrufs / `bewilligungsrahmen`; ohne Bewilligungsrahmen der übergebene Wert. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K28: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `beihilfefrei` | K7 – Beihilfefrei und Trennungsrechnung (Wahrheitswert) | Beihilfefrei; Aliase `beihilfefrei`, `STAATLBEIH`, `beihilfefreiheit`, ergänzt aus `beihilferegelung` („keine beihilfe\|beihilfefrei“). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K7: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `erstes_vorhaben` | K11 – Kein früheres Vorhaben des Begünstigten (Wahrheitswert) | Der Begünstigte hatte kein früheres Vorhaben (`len(vh) == 0` im Bewertungsschritt). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`); `tools/capture_flowinvoice_verwk_scores.py` | optional | K11: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `eu_vergaberelevant` | K24 – EU-weite Vergabe (Wahrheitswert) | EU-vergaberelevant; Aliase `eu_vergaberelevant`, `eu_vergabeverfahren`, `EU_VERGABE`, oder `vergabeverfahren` enthält „eu\|europa\|oberschwelle“, oder FPG aus `eu_vergaberelevante_fpg` bei Hochschule/Forschung. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K24: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `fpg` | K2 – FPG 1008/1009/1010 (Text) | Förderprogrammgruppe (Maßnahme, z. B. 956/960/973). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`) | optional | K2: Spalte fehlt → wie leerer Wert; leer → Schlüssel fehlt im Datensatz: Ersatztext `""` (kein Treffer); Wert `None`: Text „None“ (kein Treffer) |
| `hat_absch` | K4 – Abschreibung (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „abschreib\|afa\|wertminderung“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K4: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_bau` | K3 – Bau (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „bau\|gebäude\|anlage\|infrastruktur“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K3: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_sach` | K26 – Sachkosten (Wahrheitswert) | Mittelabruf enthält einen direkten Beleg (siehe `n_direct`), dessen Kostenart nicht „personal\|gemeinkost\|pauschal\|einnahm“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K26: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_sachleist` | K5 – Sachleistung (Wahrheitswert) | Mittelabruf enthält einen Beleg, dessen Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) „sachleist“ enthält. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K5: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `hat_vergabe` | K23 – Vergabe vorhanden (Wahrheitswert) | Mittelabruf enthält einen Beleg mit nicht leerer `vergabenummer`, oder er ist EU-vergaberelevant. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K23: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `n_direct` | K25 – Direkte Belege > 80 (Zahl) | Anzahl direkter Belege des Mittelabrufs: Kostenart-Text (`kostenart_auswertung_bezeichnung`, `kostenart_bezeichnung`, `KOWG`) ohne „gemeinkost\|pauschal\|sek\|standardeinheits\|personal“. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K25: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `oeffentlich_rechtlich` | K13 – Öffentlich-rechtlich (Wahrheitswert) | Öffentlich-rechtlicher Träger; Aliase `oeffentlich_rechtlich`, `RECHTSTRAEGER`, `rechtsform_oeffentlich`, oder Hochschule. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K13: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `oeffentlicher_auftraggeber` | K14 – Öffentlicher Auftraggeber (Wahrheitswert) | Öffentlicher Auftraggeber; Aliase `oeffentlicher_auftraggeber`, `oeff_auftraggeber`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K14: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `offene_auflagen` | K10 – Offene Auflagen (Wahrheitswert) | Offene Auflagen; Aliase `offene_auflagen`, `OFFAUF`, `offene_auflage`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K10: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `prior_families` | K20 – Frühere Familie Vergabe (Codeliste)<br>K21 – Frühere Familie Beihilfe (Codeliste)<br>K22 – Sonstige frühere Familie (Codeliste) | Menge der Feststellungsfamilien früherer Kürzungen (z. B. „Vergaberecht“, „Beihilferecht“). Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`) | optional | K20: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`)<br>K21: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`)<br>K22: Spalte fehlt → wie leerer Wert; leer → leere Menge (kein Treffer); keine Liste → Abbruch (`InputError`) |
| `prior_k` | K12 – Frühere Kürzung > 0 (Zahl) | Summe der Kürzungen früherer Mittelabrufe bzw. Vorjahre des Begünstigten und Vorhabens. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`) | optional | K12: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `prior_q` | K16 – Frühere Quote > 0 (Zahl)<br>K17 – Frühere Quote 25–50 (Zahl)<br>K18 – Frühere Quote > 50 (Zahl)<br>K19 – Frühere Quote unter 5 (Zahl) | Frühere Kürzungsquote in Prozent: `prior_k / (prior_b + prior_k) · 100`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`score_mittelabrufe`) | optional | K16: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K17: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K18: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K19: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `projekt_budget` | K8 – Projektbudget 1,5 Mio – 5 Mio (Zahl)<br>K9 – Projektbudget > 5 Mio (Zahl) | Projektbudget: Maximum von `bewilligungsrahmen` (sonst `bruttobetrag`) je Vorhaben, ersatzweise die Bruttosumme des Mittelabrufs. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K8: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`)<br>K9: Spalte fehlt → wie leerer Wert; leer → Ersatzwert `0`, kein Treffer (ebenso bei `""` oder 0); NaN → kein Treffer; nicht numerischer Text → Abbruch (`InputError`) |
| `trennungsrechnung_erforderlich` | K7 – Beihilfefrei und Trennungsrechnung (Wahrheitswert) | Trennungsrechnung erforderlich; Aliase `trennungsrechnung_erforderlich`, `trennungsrechnung`, `TRENNRECH`. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K7: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |
| `verbundvorhaben` | K1 – Verbundvorhaben (Wahrheitswert) | Verbundvorhaben; Aliase `verbundvorhaben`, `verbund`, `AKTZEIEXT2`, auch auf Belegebene. Beleg: flowinvoice@fb2d185 `backend/app/verwk/pipeline/rbvk_wibank_scorer.py` (`_normalise_sources`) | optional | K1: Spalte fehlt → wie leerer Wert; leer → nicht zutreffend (keine Punkte) |

## flowinvoice.risk_checker 2026.09.2

Status `APPROVED`, Fingerabdruck `49123fb9b23b27b0e4881ffa502b2951429873b6c46046eae178ceff39e52860`.
Eingabevertrag laut Profil: Ein Datensatz je Rechnung wie RiskAssessmentRequest, Kontext flach als context.<feld> (flatten_record).

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `beneficiary_name` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert rechts) | Name des Begünstigten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `beneficiary_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert rechts) | USt-IdNr. des Begünstigten (für Selbstrechnungs-Prüfung). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `context.median_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Median) | Median-Betrag vergleichbarer Rechnungen. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.project_end` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ende des Vergleichszeitraums) | Projektende. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.project_start` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Beginn des Vergleichszeitraums) | Projektbeginn. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.std_deviation` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Standardabweichung) | Standardabweichung der Beträge. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.total_vendor_count` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Nenner) | Gesamtanzahl verschiedener Lieferanten (zur Deutung siehe Hinweis der Regel VENDOR_CLUSTERING). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_frequency` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Zähler) | Anzahl Rechnungen dieses Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_invoices` | SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Rechnungsliste (Einträge mit `net_amount`, `invoice_date`)) | Alle Rechnungen desselben Lieferanten im Projekt; je Eintrag (`VendorInvoiceSummary`) `net_amount` „Nettobetrag“ und `invoice_date` „Rechnungsdatum“. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei leerer Liste); Eintrag ohne `net_amount` oder `invoice_date` → Abbruch (`InputError`) |
| `description` | ROUND_AMOUNT – Runder Pauschalbetrag (Beschreibung (Pauschal-Begriffe))<br>NO_PROJECT_REFERENCE – Leistungsbeschreibung ohne Projektbezug (Text (Muster)) | Leistungsbeschreibung (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | ROUND_AMOUNT: Spalte fehlt → wie leerer Wert; leer → keine Begriffe gefunden (Schwere LOW)<br>NO_PROJECT_REFERENCE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal |
| `invoice_date` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ersatzdatum)<br>SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.2)) | Rechnungsdatum (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → fehlt auch das Leistungsdatum und ist der Vergleichszeitraum angegeben → Abbruch (`InputError`)<br>SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; ohne Treffer → unbestimmt |
| `invoice_recipient` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert links) | Rechnungsempfänger. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `net_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Betrag)<br>ROUND_AMOUNT – Runder Pauschalbetrag (Betrag) | Nettobetrag (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | **Pflicht**, auch der Wert (HIGH_AMOUNT, ROUND_AMOUNT) | HIGH_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet)<br>ROUND_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet) |
| `service_period_end` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsende) | Ende Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `service_period_start` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsbeginn) | Beginn Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `supplier_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert links) | USt-IdNr. des Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `vendor_name` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Textbaustein `vendor_name`)<br>SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Textbaustein `vendor_name`) | Lieferantenname (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text<br>SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text |

## flowinvoice.risk_checker 2026.09.3

Status `APPROVED`, Fingerabdruck `495e37d3e1a7b16c176535905ccb060f6bd091b25639ab37fb29e22e43ad29e5`.
Eingabevertrag laut Profil: Ein Datensatz je Rechnung wie RiskAssessmentRequest, Kontext flach als context.<feld> (flatten_record).

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `beneficiary_name` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert rechts) | Name des Begünstigten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `beneficiary_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert rechts) | USt-IdNr. des Begünstigten (für Selbstrechnungs-Prüfung). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `context.median_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Median) | Median-Betrag vergleichbarer Rechnungen. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.project_end` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ende des Vergleichszeitraums) | Projektende. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.project_start` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Beginn des Vergleichszeitraums) | Projektbeginn. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.std_deviation` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Standardabweichung) | Standardabweichung der Beträge. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.total_vendor_count` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Nenner) | Gesamtanzahl verschiedener Lieferanten (zur Deutung siehe Hinweis der Regel VENDOR_CLUSTERING). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_frequency` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Zähler) | Anzahl Rechnungen dieses Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_invoices` | SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Rechnungsliste (Einträge mit `net_amount`, `invoice_date`)) | Alle Rechnungen desselben Lieferanten im Projekt; je Eintrag (`VendorInvoiceSummary`) `net_amount` „Nettobetrag“ und `invoice_date` „Rechnungsdatum“. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei leerer Liste); Eintrag ohne `net_amount` oder `invoice_date` → Abbruch (`InputError`) |
| `description` | ROUND_AMOUNT – Runder Pauschalbetrag (Beschreibung (Pauschal-Begriffe))<br>NO_PROJECT_REFERENCE – Leistungsbeschreibung ohne Projektbezug (Text (Muster)) | Leistungsbeschreibung (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | ROUND_AMOUNT: Spalte fehlt → wie leerer Wert; leer → keine Begriffe gefunden (Schwere LOW)<br>NO_PROJECT_REFERENCE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal |
| `invoice_date` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ersatzdatum)<br>SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.3)) | Rechnungsdatum (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → fehlt auch das Leistungsdatum und ist der Vergleichszeitraum angegeben → Abbruch (`InputError`)<br>SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; ohne Treffer → unbestimmt |
| `invoice_recipient` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert links) | Rechnungsempfänger. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `net_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Betrag)<br>ROUND_AMOUNT – Runder Pauschalbetrag (Betrag) | Nettobetrag (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | **Pflicht**, auch der Wert (HIGH_AMOUNT, ROUND_AMOUNT) | HIGH_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet)<br>ROUND_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet) |
| `service_period_end` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsende) | Ende Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `service_period_start` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsbeginn) | Beginn Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `supplier_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert links) | USt-IdNr. des Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `vendor_name` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Textbaustein `vendor_name`)<br>SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Textbaustein `vendor_name`) | Lieferantenname (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text<br>SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text |

## flowinvoice.risk_checker fb2d18568d2e

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `063eebd38a04e685028eaa1ef47c1a1b0970ba9ff50b93f8544ddd2e9f0ac169`.
Eingabevertrag laut Profil: Ein Datensatz je Rechnung wie RiskAssessmentRequest, Kontext flach als context.<feld> (flatten_record).

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `beneficiary_name` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert rechts) | Name des Begünstigten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `beneficiary_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert rechts) | USt-IdNr. des Begünstigten (für Selbstrechnungs-Prüfung). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `context.median_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Median) | Median-Betrag vergleichbarer Rechnungen. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.project_end` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ende des Vergleichszeitraums) | Projektende. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.project_start` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Beginn des Vergleichszeitraums) | Projektbeginn. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → keine Prüfung (kein Merkmal) |
| `context.std_deviation` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Standardabweichung) | Standardabweichung der Beträge. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | HIGH_AMOUNT: Spalte fehlt → wie leerer Wert; leer → nur der absolute Schwellenwert wird geprüft (ebenso bei 0) |
| `context.total_vendor_count` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Nenner) | Gesamtanzahl verschiedener Lieferanten (zur Deutung siehe Hinweis der Regel VENDOR_CLUSTERING). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_frequency` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Zähler) | Anzahl Rechnungen dieses Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei 0) |
| `context.vendor_invoices` | SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Rechnungsliste (Einträge mit `net_amount`, `invoice_date`)) | Alle Rechnungen desselben Lieferanten im Projekt; je Eintrag (`VendorInvoiceSummary`) `net_amount` „Nettobetrag“ und `invoice_date` „Rechnungsdatum“. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskContext`, flach als `context.<feld>`) | optional | SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso bei leerer Liste); Eintrag ohne `net_amount` oder `invoice_date` → Abbruch (`InputError`) |
| `description` | ROUND_AMOUNT – Runder Pauschalbetrag (Beschreibung (Pauschal-Begriffe))<br>NO_PROJECT_REFERENCE – Leistungsbeschreibung ohne Projektbezug (Text (Muster)) | Leistungsbeschreibung (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | ROUND_AMOUNT: Spalte fehlt → wie leerer Wert; leer → keine Begriffe gefunden (Schwere LOW)<br>NO_PROJECT_REFERENCE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal |
| `invoice_date` | OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Ersatzdatum) | Rechnungsdatum (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → fehlt auch das Leistungsdatum und ist der Vergleichszeitraum angegeben → Abbruch (`InputError`) |
| `invoice_recipient` | RECIPIENT_MISMATCH – Rechnungsempfänger weicht vom Begünstigten ab (Vergleichswert links) | Rechnungsempfänger. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | RECIPIENT_MISMATCH: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `net_amount` | HIGH_AMOUNT – Ungewöhnlich hoher Einzelbetrag (Betrag)<br>ROUND_AMOUNT – Runder Pauschalbetrag (Betrag) | Nettobetrag (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | **Pflicht**, auch der Wert (HIGH_AMOUNT, ROUND_AMOUNT) | HIGH_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet)<br>ROUND_AMOUNT: Spalte fehlt → Abbruch (`InputError`); leer → Abbruch (`InputError`, Zahl erwartet) |
| `service_period_end` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsende) | Ende Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `service_period_start` | MISSING_PERIOD – Fehlender Leistungszeitraum (Angabe)<br>OUTSIDE_PROJECT_PERIOD – Leistung außerhalb des Projektzeitraums (Leistungsbeginn) | Beginn Leistungszeitraum. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | MISSING_PERIOD: Spalte fehlt → wie leerer Wert; leer → zählt als fehlend (auch `""` oder 0); fehlen alle Felder → Merkmal<br>OUTSIDE_PROJECT_PERIOD: Spalte fehlt → wie leerer Wert; leer → `invoice_date` wird verwendet |
| `supplier_vat_id` | SELF_INVOICE – Selbstrechnung (gleiche USt-IdNr.) (Vergleichswert links) | USt-IdNr. des Lieferanten. Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | SELF_INVOICE: Spalte fehlt → wie leerer Wert; leer → kein Merkmal (ebenso, wenn der andere Wert fehlt) |
| `vendor_name` | VENDOR_CLUSTERING – Auffällige Lieferantenhäufung (Textbaustein `vendor_name`)<br>SPLIT_INVOICE – Verdacht auf Rechnungssplitting (Textbaustein `vendor_name`) | Lieferantenname (Pflichtfeld im Schema). Beleg: flowinvoice@fb2d185 `backend/app/schemas/risk.py` (`RiskAssessmentRequest`) | optional | VENDOR_CLUSTERING: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text<br>SPLIT_INVOICE: Spalte fehlt → wie leerer Wert; leer → erscheint als „None“ im Text |

## riskanalysis.legacy b5c523bf7eaa

Status `LEGACY_CHARACTERIZED`, Fingerabdruck `a9736bbd2450e6dc4fa7f1ce91e1596799485a88c57b2221c65768b74b22560e`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF02 – Beleg knapp unter Vergabe-/Schwellenwert (Betrag)<br>RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF02, RF08, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF02: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |

## riskanalysis.year_bound 2026.09.1

Status `CANDIDATE_HUMAN_DECISION_REQUIRED`, Fingerabdruck `f9b4a01323bd0350c8c77117172565c857b34a1b400544f3696290f15ab395b1`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF02 – Beleg knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Betrag)<br>RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF02, RF08, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF02: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `rechnungsdatum_dt` | RF02 – Beleg knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.2)) | Rechnungsdatum als Datum (`pd.to_datetime` der Quellspalte `rechnungsdatum`); Stichtag der jahresbezogenen EU-Schwelle. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; trifft keine Profilschwelle → unbestimmt |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |

## riskanalysis.year_bound 2026.09.2

Status `APPROVED`, Fingerabdruck `72696f5609d6c29388f0d42d76fb66bd11780fbadee69c4a646ace918102d41b`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `nettobetrag` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Betrag)<br>RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag) | Nettobetrag des Belegs. `t_Auswertung_Gesamt` hat keine Nettospalte; riskanalysis übernimmt die Spalte „Gesamt Netto“ der hessischen Belegliste als `nettobetrag` und leitet nichts aus dem Bruttobetrag ab. Beleg: riskanalysis@9a5b624 `backend/app/pipeline/mdb_loader.py` (`load_raw`); `docs/behavior-changes.md` (K2, K2a) | **Pflicht** (RF02, RF08) | RF02: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `rechnungsdatum_dt` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.2)) | Rechnungsdatum als Datum (`pd.to_datetime` der Quellspalte `rechnungsdatum`); Stichtag der jahresbezogenen EU-Schwelle. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; trifft keine Profilschwelle → unbestimmt |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |

## riskanalysis.year_bound 2026.09.3

Status `APPROVED`, Fingerabdruck `57079051f6ad1550aef7aed53bf7c12661c5ee5ecd07a2d23025a8219a8164a0`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `nettobetrag` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Betrag)<br>RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag) | Nettobetrag des Belegs. `t_Auswertung_Gesamt` hat keine Nettospalte; riskanalysis übernimmt die Spalte „Gesamt Netto“ der hessischen Belegliste als `nettobetrag` und leitet nichts aus dem Bruttobetrag ab. Beleg: riskanalysis@9a5b624 `backend/app/pipeline/mdb_loader.py` (`load_raw`); `docs/behavior-changes.md` (K2, K2a) | **Pflicht** (RF02, RF08) | RF02: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `rechnungsdatum_dt` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.2)) | Rechnungsdatum als Datum (`pd.to_datetime` der Quellspalte `rechnungsdatum`); Stichtag der jahresbezogenen EU-Schwelle. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; trifft keine Profilschwelle → unbestimmt |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |

## riskanalysis.year_bound 2026.09.4

Status `APPROVED`, Fingerabdruck `266e8c9e02eadb7514aed8b0121f07d9140a2044c43df28b00197ea3109fb3e2`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `nettobetrag` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Betrag)<br>RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag) | Nettobetrag des Belegs. `t_Auswertung_Gesamt` hat keine Nettospalte; riskanalysis übernimmt die Spalte „Gesamt Netto“ der hessischen Belegliste als `nettobetrag` und leitet nichts aus dem Bruttobetrag ab. Beleg: riskanalysis@9a5b624 `backend/app/pipeline/mdb_loader.py` (`load_raw`); `docs/behavior-changes.md` (K2, K2a) | **Pflicht** (RF02, RF08) | RF02: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `rechnungsdatum_dt` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.3)) | Rechnungsdatum als Datum (`pd.to_datetime` der Quellspalte `rechnungsdatum`); Stichtag der jahresbezogenen EU-Schwelle. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; trifft keine Profilschwelle → unbestimmt |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |

## riskanalysis.year_bound 2026.09.5

Status `APPROVED`, Fingerabdruck `dd32ede2494116ad3fc4cf5a01a479c2abb6ed6b479fbc3e8a1c04d656610571`.

| Feld | Regeln (Code – Bezeichnung, Rolle) | Bedeutung | Pflicht/optional | Verhalten bei fehlender Spalte / leerem Wert |
|---|---|---|---|---|
| `abweichungen_betrag` | RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Kürzungsbetrag) | Kürzung = Bruttobetrag − anerkannter Betrag (finanzielle Feststellung). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF12: Spalte fehlt → Ersatzwert `0.0`; leer → Ersatzwert `0.0` (auch nicht numerischer Text) |
| `antrag` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Vorhaben)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Vorhaben) | Vorhaben-ID: `t_Auswertung_Antragsnummer` als Ganzzahl (eindeutige, 8-stellige Vorhaben-ID). Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg nur im Gruppenkriterium (Betrag zählt, Vorhaben nicht)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nur zur Gruppe, kein Merkmal |
| `Anzahl_ungueltige_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl Teilmenge) | Anzahl der ungültigen Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `Anzahl_Versionen` | RF11 – Mittelabruf-Historie mit überwiegend ungültigen Versionen (Anzahl gesamt) | Anzahl der Versionen in der Mittelabruf-Historie (RF11). `load_raw` erzeugt die Spalte nicht. Beleg: riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`_compute_rf11`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF11: Spalte fehlt → kein Merkmal für alle Datensätze; leer → gilt als 0 (auch nicht numerischer Text) |
| `auszahlungsdauer_tage` | RF13 – Auszahlung eines Mittelabrufs nach mehr als 80 Tagen (Problemursache zu verifizieren) (Zahl) | Auszahlungsdauer des Mittelabrufs in Tagen. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `0.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF13: Spalte fehlt → Ersatzwert `0` (kein Merkmal); leer → Ersatzwert `0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `bruttobetrag` | RF01 – Auffällig runde Beträge (glatte Tausender) (Betrag)<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Betrag (Summen))<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Betrag (Quote))<br>Zusammenfassung – Volumen je Merkmal (`red_flag_summary`) (Betrag) | Geltend gemachter Betrag eines Belegs (vor Prüfung). Quellspalte `bruttobetrag` in `t_Auswertung_Gesamt`. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | **Pflicht** (RF01, RF10) | RF01: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0` (kein Merkmal); Text statt Zahl → Abbruch (`InputError`)<br>RF10: Spalte fehlt → Abbruch (`InputError`); leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Ersatzwert `0.0`; Text statt Zahl → Abbruch (`InputError`)<br>Zusammenfassung: Spalte fehlt → Volumen 0; leer → nicht gezählt; Text statt Zahl → Abbruch (`InputError`) |
| `Gruppennummer` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Gruppe)<br>RF12 – Hohe historische Kürzungsquote des Begünstigten (> 10 %, leave-one-out; Schwelle aus rbvk-Kriterium 19 übernommen, nicht datenkalibriert) (Gruppe) | Konzern-/Verflechtungsgruppe des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`); riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF10: Spalte fehlt → nur das Kriterium je Vorhaben und Auftragnehmer; leer → Beleg nur im Kriterium je Vorhaben und Auftragnehmer<br>RF12: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg bleibt unberücksichtigt |
| `indikator_erreichungsquote` | RF14 – Indikator-Erreichungsquote unter 80 % (Zahl) | Erreichungsquote des Indikators als Anteil. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann `1.0`. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF14: Spalte fehlt → Ersatzwert `1.0` (kein Merkmal); leer → Ersatzwert `1.0` (kein Merkmal); nicht numerischer Text gilt als leer |
| `kostenart_auswertung_bezeichnung` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“))<br>RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Kostenart (nicht vergaberelevant bei Muster „personal\|gemeinkost\|pauschal\|abschreibung“)) | Aggregierte Kostenart (z. B. „Bauliche Investitionen“, „Personalausgaben“). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant<br>RF10: Spalte fehlt → alle Datensätze gelten als vergaberelevant; leer → gilt als vergaberelevant |
| `Name` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name links (Begünstigter)) | Name des Begünstigten; aus Tabelle `t_Namen` über die Antragsnummer zugeordnet. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`); riskanalysis@b5c523b `backend/app/pipeline/red_flags.py` (`compute_red_flags`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
| `nettobetrag` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Betrag)<br>RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Betrag) | Nettobetrag des Belegs. `t_Auswertung_Gesamt` hat keine Nettospalte; riskanalysis übernimmt die Spalte „Gesamt Netto“ der hessischen Belegliste als `nettobetrag` und leitet nichts aus dem Bruttobetrag ab. Beleg: riskanalysis@9a5b624 `backend/app/pipeline/mdb_loader.py` (`load_raw`); `docs/behavior-changes.md` (K2, K2a) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → unbestimmt („Nettobetrag fehlt in der Quelle“); Text statt Zahl → Abbruch (`InputError`)<br>RF08: Spalte fehlt → wie leerer Wert; leer → unbestimmt („Nettobetrag fehlt in der Quelle“) nur ohne echte Vergabekennung bei vergaberelevanter Kostenart, sonst kein Merkmal; Text statt Zahl → Abbruch (`InputError`) |
| `payee_canonical` | RF10 – Wiederholte Beauftragung desselben Auftragnehmers (Konzentration) (Auftragnehmer (kanonisch)) | Normalisierter Name des Zahlungsempfängers (`normalize_name` auf `zahlungsempfaenger`). Beleg: riskanalysis@b5c523b `backend/app/pipeline/payee_normalizer.py` (`add_payee_columns`) | optional | RF10: Spalte fehlt → kein Merkmal für alle Datensätze; leer → Beleg zählt nicht |
| `_pseudonym_rf09` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (übernommene Vorberechnung des Merkmals) | In pseudonymisierten Beständen vor dem Ersetzen der Namen berechnetes RF09-Ergebnis (`_name_match(Name, zahlungsempfaenger) >= 0.85`). Beleg: riskanalysis@b5c523b `backend/app/services/pseudonymization.py` | optional | RF09: Spalte fehlt → Ähnlichkeitsschwelle entscheidet; leer → kein Merkmal; sonst wird der Wahrheitswert übernommen (Text „False“ gilt als wahr, RK-L03) |
| `rechnungsdatum_dt` | RF02 – Beleg (netto) knapp unter Vergabe-/Schwellenwert (EU-Schwelle jahresbezogen) (Stichtag der EU-Schwelle (procurement.hvtg 2026.09.3)) | Rechnungsdatum als Datum (`pd.to_datetime` der Quellspalte `rechnungsdatum`); Stichtag der jahresbezogenen EU-Schwelle. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF02: Spalte fehlt → wie leerer Wert; leer → EU-Schwelle nicht bestimmbar; trifft keine Profilschwelle → unbestimmt |
| `sanktionslisten_status` | RF15 – Möglicher Sanktionslisten-Treffer des Rechnungsstellers (Text) | Ergebnis der Sanktionslistenprüfung des Rechnungsstellers. Im originalen MDB-Bestand nicht vorhanden; `load_raw` setzt dann „nicht geprüft“. Beleg: riskanalysis@b5c523b `backend/app/pipeline/mdb_loader.py` (`load_raw`) | optional | RF15: Spalte fehlt → Ersatzwert `"kein Treffer"` (kein Merkmal); leer → kein Merkmal |
| `vergabenummer` | RF08 – Beleg netto > 25 T€ ohne echte Vergabe-Kennung (vergaberelevante Kostenart) (Vergabekennung) | Vergabeverfahren-Referenz des Belegs. Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | optional | RF08: Spalte fehlt → gilt als fehlende Vergabekennung; leer → gilt als fehlende Vergabekennung (auch `""`, `"nan"`, ohne Groß-/Kleinschreibung); Platzhalter nach Muster `^\s*0([=\s.\-].*)?$` ebenso |
| `zahlungsempfaenger` | RF09 – Begünstigter = Auftragnehmer (Selbstbeauftragung) (Name rechts (Auftragnehmer)) | Rechnungssteller / Auftragnehmer eines Belegs (≠ Begünstigter). Beleg: riskanalysis@b5c523b `backend/app/services/report.py` (`FELD_ERKLAERUNG`) | **Pflicht** (RF09) | RF09: Spalte fehlt → Abbruch (`InputError`); leer → Ähnlichkeit 0 (kein Merkmal aus dem Namensvergleich) |
