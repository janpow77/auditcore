/** Darstellungshilfen aus `@flowaudit/kanban-core` (gleich in Vue und React). */
export {
  BADGE_COLORS, CARD_COLORS, COLUMN_COLORS, MAX_CARD_IMAGE_BYTES, PRIORITY_TONES,
  badgePrefix, badgeStyle, cardAge, cardStyle, fileSize, initials, preview, relativeTime, textOn,
  type AgeKey, type PriorityTone, type RelativeKey,
} from '@flowaudit/kanban-core'
