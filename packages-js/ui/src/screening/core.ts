/**
 * Kern aus `@auditcore/ui-core` unter den bisherigen Namen der Vue-Fassung
 * (`formatDate` der Screening-Ansicht). Fachlogik liegt nur im Kern.
 */
export * from '@auditcore/ui-core'
export { formatScreeningDate as formatDate } from '@auditcore/ui-core'
