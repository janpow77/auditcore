/**
 * Kern aus `@flowaudit/ui-core` unter den bisherigen Namen der Vue-Fassung
 * (`formatDate` der Screening-Ansicht). Fachlogik liegt nur im Kern.
 */
export * from '@flowaudit/ui-core'
export { formatScreeningDate as formatDate } from '@flowaudit/ui-core'
